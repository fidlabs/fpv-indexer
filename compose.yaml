services:
  database:
    image: postgres:18
    restart: always
    shm_size: 128mb
    environment:
      PGUSER: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: db
    ports:
      - 54001:5432
